'use client';

export type UserProfileT = {
  /**
   * The store type is the store itself.
   */
  onSignIn: any;
};

export const UserProfile = ({ onSignIn }: UserProfileT) => {

  return (
    <div>
      1
    </div>
  );
};
